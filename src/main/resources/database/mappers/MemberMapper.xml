<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.iu.home.member.MemberMapper">

	<!-- 로그인 -->
	<select id="getLogin" parameterType="MemberVO" resultMap="getLoginResult">
		SELECT * FROM MEMBER M, R.*
			INNER JOIN MEMBER_ROLE MR
				ON M.ID = MR.ID
			INNER JOIN ROLE R 
				ON MR.ROLENUM = R.ROLENUM
		WHERE M.ID = #{id} AND M.PW = #{pw}
	</select>

	<resultMap type="MemberVO" id="getLogin">
		<id column="ID" property="id"/>
		<result column="name" property="name"/>
		<result column="email" property="email"/>
		
		<collection property="roleVOs" javaType="List" ofType="RoleVO">
			<id column="ROLENUM" property="roleNum"/>
			<id column="ROLENAME" property="roleName"/>
		</collection>
	</resultMap>
	
	
	
	<!-- 회원가입 -->
	<insert id="setJoin" parameterType="MemberVO">
		INSERT INTO MEMBER(ID, PASSWORD, NAME, EMAIL)
		VALUES (#{id}, #{password}, #{name}, #{email})
	</insert>
	
	<insert id="setRole" parameterType="MemberVO">
		INSERT INTO MEMBER_ROLE (ID, ROLENUM)
		VALUES (#{id}, #{memberNum})
		
		INSERT INTO MEMBER_ROLE VALUES (#{id},
			(SELECT NUM FROM MEMBER WHERE #{memberNum}=1))
	</insert>
	
	
</mapper>