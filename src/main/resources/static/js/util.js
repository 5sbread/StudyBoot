//계속 사용하는 함수는 따로 파일 만들기
function nullCheck(data, dest, kind){
    let dest = f
    if(data == null || data==''){
        $(data).
        return false;
    }else {
        return true;
    }
}

function equal(data, checkData){
    if(data == null || data==''){
        return true;
    }else {
        return false;
    }
}

